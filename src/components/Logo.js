import { Typography } from "@mui/material";
import { Box } from "@mui/system"

const Logo = () => (
  <Box sx={{fontFamily: 'Manrope'}}>
    <Typography variant="h4" component='h1'>TeeRex</Typography>
  </Box>
)

export default Logo;