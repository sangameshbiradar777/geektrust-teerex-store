import { Box, TextField } from "@mui/material";

const Search = () => {
  return (
    <Box component="form">
      <TextField label='Search over 100+ products' hiddenLabel variant="outlined"  />
    </Box>
  );
};

export default Search;
